import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Yonas Million - Software Engineer & Designer",
  description: "Portfolio of Yonas Million, a software engineer and graphics designer showcasing projects and skills.",
  keywords: "software engineer, graphics designer, portfolio, web development",
  viewport: "width=device-width, initial-scale=1",
  openGraph: {
    title: "Yonas Million - Software Engineer & Designer",
    description: "Portfolio of Yonas Million, a software engineer and graphics designer",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
      </head>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}